<h1>All Tags</h1>

<ul id="tags">
	<% @tag.each do |tag| %>
		<li>
			<%= tag.name %>
			<!-- <% link_to "Delete", tag_path(@tag), method: :delete, data: {confirm: "Really delete this tag?"} %> --> <!-- this needs to be worked on -->
		</li>
	<% end %>
</ul><br>

<%= link_to "<< Back to Articles List", articles_path %>